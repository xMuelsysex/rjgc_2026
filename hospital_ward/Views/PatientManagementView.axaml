<UserControl
x:Class="MyFirstApp.Views.PatientManagementView"
xmlns="https://github.com/avaloniaui"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
xmlns:sys="clr-namespace:System;assembly=mscorlib"
xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
xmlns:vm="using:MyFirstApp.ViewModels"
d:DesignHeight="600"
d:DesignWidth="900"
x:DataType="vm:PatientManagementViewModel"
mc:Ignorable="d">

    <Border Background="#F9FAFB" CornerRadius="16" Margin="16" Padding="24">
        <Grid RowDefinitions="Auto, Auto, *, Auto">
            <TextBlock
            Margin="0,0,0,16"
            FontSize="28"
            FontWeight="Bold"
            Foreground="#111827"
            Text="患者管理" />

            <StackPanel
            Grid.Row="1"
            Margin="0,0,0,16"
            Orientation="Horizontal"
            Spacing="10">
                <TextBox
                Width="240"
                Text="{Binding SearchText}"
                Watermark="搜索患者姓名/床位..."
                CornerRadius="8" />
                <Button
                Padding="16,8"
                Background="#10B981"
                CornerRadius="8"
                Command="{Binding AddCommand}"
                Content="添加患者"
                FontWeight="SemiBold"
                Foreground="White" />
                <Button
                Padding="16,8"
                Background="#3B82F6"
                CornerRadius="8"
                Command="{Binding EditCommand}"
                Content="编辑"
                FontWeight="SemiBold"
                Foreground="White" />
                <Button
                Padding="16,8"
                Background="#EF4444"
                CornerRadius="8"
                Command="{Binding DeleteCommand}"
                Content="删除"
                FontWeight="SemiBold"
                Foreground="White" />
            </StackPanel>

            <DataGrid
            Grid.Row="2"
            AutoGenerateColumns="False"
            BorderBrush="#E5E7EB"
            BorderThickness="1"
            CanUserResizeColumns="True"
            CornerRadius="8"
            GridLinesVisibility="Horizontal"
            IsReadOnly="True"
            ItemsSource="{Binding FilteredItems}"
            SelectedItem="{Binding SelectedItem}">
                <DataGrid.Columns>
                    <DataGridTextColumn
                    Width="80"
                    Binding="{Binding Name}"
                    Header="姓名" />
                    <DataGridTextColumn
                    Width="50"
                    Binding="{Binding Gender}"
                    Header="性别" />
                    <DataGridTextColumn
                    Width="50"
                    Binding="{Binding Age}"
                    Header="年龄" />
                    <DataGridTextColumn
                    Width="120"
                    Binding="{Binding Phone}"
                    Header="联系电话" />
                    <DataGridTextColumn
                    Width="90"
                    Binding="{Binding BedNumber}"
                    Header="床位号" />
                    <DataGridTextColumn
                    Width="80"
                    Binding="{Binding DepartmentName}"
                    Header="科室" />
                    <DataGridTextColumn
                    Width="60"
                    Binding="{Binding Status}"
                    Header="状态" />
                </DataGrid.Columns>
            </DataGrid>

            <!--  编辑面板  -->
            <Border
            Grid.Row="3"
            Classes="glass"
            Margin="0,16,0,0"
            Padding="20"
            IsVisible="{Binding IsEditing}">
                <StackPanel Spacing="16">
                    <TextBlock
                    FontSize="18"
                    FontWeight="Bold"
                    Foreground="#111827"
                    Text="编辑患者信息" />
                    <WrapPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <StackPanel Margin="0,0,20,12" Spacing="6">
                            <TextBlock
                            FontSize="13"
                            Foreground="#6B7280"
                            FontWeight="Medium"
                            Text="姓名" />
                            <TextBox Width="120" CornerRadius="6" Text="{Binding EditName}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,20,12" Spacing="6">
                            <TextBlock
                            FontSize="13"
                            Foreground="#6B7280"
                            FontWeight="Medium"
                            Text="性别" />
                            <ComboBox Width="80" CornerRadius="6" SelectedItem="{Binding EditGender}">
                                <x:String>男</x:String>
                                <x:String>女</x:String>
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Margin="0,0,20,12" Spacing="6">
                            <TextBlock
                            FontSize="13"
                            Foreground="#6B7280"
                            FontWeight="Medium"
                            Text="年龄" />
                            <NumericUpDown
                            Width="100"
                            CornerRadius="6"
                            Maximum="150"
                            Minimum="0"
                            Value="{Binding EditAge}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,20,12" Spacing="6">
                            <TextBlock
                            FontSize="13"
                            Foreground="#6B7280"
                            FontWeight="Medium"
                            Text="联系电话" />
                            <TextBox Width="140" CornerRadius="6" Text="{Binding EditPhone}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,20,12" Spacing="6">
                            <TextBlock
                            FontSize="13"
                            Foreground="#6B7280"
                            FontWeight="Medium"
                            Text="身份证号" />
                            <TextBox Width="180" CornerRadius="6" Text="{Binding EditIdCard}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,20,12" Spacing="6">
                            <TextBlock
                            FontSize="13"
                            Foreground="#6B7280"
                            FontWeight="Medium"
                            Text="床位号" />
                            <TextBox Width="120" CornerRadius="6" Text="{Binding EditBedNumber}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,20,12" Spacing="6">
                            <TextBlock
                            FontSize="13"
                            Foreground="#6B7280"
                            FontWeight="Medium"
                            Text="科室" />
                            <TextBox Width="120" CornerRadius="6" Text="{Binding EditDepartmentName}" />
                        </StackPanel>
                        <StackPanel Margin="0,0,20,12" Spacing="6">
                            <TextBlock
                            FontSize="13"
                            Foreground="#6B7280"
                            FontWeight="Medium"
                            Text="状态" />
                            <ComboBox Width="100" CornerRadius="6" SelectedItem="{Binding EditStatus}">
                                <x:String>在院</x:String>
                                <x:String>出院</x:String>
                            </ComboBox>
                        </StackPanel>
                    </WrapPanel>

                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <Button
                        Padding="24,8"
                        Background="#10B981"
                        CornerRadius="8"
                        Command="{Binding SaveCommand}"
                        Content="保存"
                        FontWeight="SemiBold"
                        Foreground="White" />
                        <Button
                        Padding="24,8"
                        Background="#9CA3AF"
                        CornerRadius="8"
                        Command="{Binding CancelCommand}"
                        Content="取消"
                        FontWeight="SemiBold"
                        Foreground="White" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
