<UserControl
    x:Class="MyFirstApp.Views.MedicationView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MyFirstApp.ViewModels"
    d:DesignHeight="600"
    d:DesignWidth="900"
    x:DataType="vm:MedicationViewModel"
    mc:Ignorable="d">

    <Border Background="#F9FAFB" CornerRadius="16" Margin="16" Padding="24">
        <Grid RowDefinitions="Auto, Auto, *, Auto">
            <TextBlock
                Margin="0,0,0,16"
                FontSize="28"
                FontWeight="Bold"
                Foreground="#111827"
                Text="用药信息" />
                
            <StackPanel
                Grid.Row="1"
                Margin="0,0,0,16"
                Orientation="Horizontal"
                Spacing="10">
                <TextBox
                    Width="240"
                    Text="{Binding SearchText}"
                    Watermark="搜索患者/药品/医生..."
                    CornerRadius="8" />
                <Button
                    Padding="16,8"
                    Background="#10B981"
                    CornerRadius="8"
                    Command="{Binding AddCommand}"
                    Content="新增记录"
                    FontWeight="SemiBold"
                    Foreground="White" />
                <Button
                    Padding="16,8"
                    Background="#3B82F6"
                    CornerRadius="8"
                    Command="{Binding EditCommand}"
                    Content="编辑"
                    FontWeight="SemiBold"
                    Foreground="White" />
                <Button
                    Padding="16,8"
                    Background="#EF4444"
                    CornerRadius="8"
                    Command="{Binding DeleteCommand}"
                    Content="删除"
                    FontWeight="SemiBold"
                    Foreground="White" />
            </StackPanel>
            
            <DataGrid
                Grid.Row="2"
                AutoGenerateColumns="False"
                BorderBrush="#E5E7EB"
                BorderThickness="1"
                CanUserResizeColumns="True"
                CornerRadius="8"
                GridLinesVisibility="Horizontal"
                IsReadOnly="True"
                ItemsSource="{Binding FilteredItems}"
                SelectedItem="{Binding SelectedItem}">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding PatientName}"
                        Header="患者姓名" />
                    <DataGridTextColumn
                        Width="*"
                        Binding="{Binding MedicationName}"
                        Header="药品名称" />
                    <DataGridTextColumn
                        Width="80"
                        Binding="{Binding Dosage}"
                        Header="剂量" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding Frequency}"
                        Header="频次" />
                    <DataGridTextColumn
                        Width="100"
                        Binding="{Binding Doctor}"
                        Header="开药医生" />
                    <DataGridTextColumn
                        Width="110"
                        Binding="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}"
                        Header="日期" />
                </DataGrid.Columns>
            </DataGrid>
            
            <Border
                Grid.Row="3"
                Classes="glass"
                Margin="0,16,0,0"
                Padding="20"
                IsVisible="{Binding IsEditing}">
                <StackPanel Spacing="16">
                    <TextBlock
                        FontSize="18"
                        FontWeight="Bold"
                        Foreground="#111827"
                        Text="编辑用药信息" />
                        
                    <StackPanel Orientation="Horizontal" Spacing="20">
                        <StackPanel Spacing="6">
                            <TextBlock
                                FontSize="13"
                                Foreground="#6B7280"
                                FontWeight="Medium"
                                Text="患者姓名" />
                            <TextBox Width="120" CornerRadius="6" Text="{Binding EditPatientName}" />
                        </StackPanel>
                        <StackPanel Spacing="6">
                            <TextBlock
                                FontSize="13"
                                Foreground="#6B7280"
                                FontWeight="Medium"
                                Text="药品名称" />
                            <TextBox Width="140" CornerRadius="6" Text="{Binding EditMedicationName}" />
                        </StackPanel>
                        <StackPanel Spacing="6">
                            <TextBlock
                                FontSize="13"
                                Foreground="#6B7280"
                                FontWeight="Medium"
                                Text="剂量" />
                            <TextBox Width="80" CornerRadius="6" Text="{Binding EditDosage}" />
                        </StackPanel>
                        <StackPanel Spacing="6">
                            <TextBlock
                                FontSize="13"
                                Foreground="#6B7280"
                                FontWeight="Medium"
                                Text="频次" />
                            <TextBox Width="100" CornerRadius="6" Text="{Binding EditFrequency}" />
                        </StackPanel>
                        <StackPanel Spacing="6">
                            <TextBlock
                                FontSize="13"
                                Foreground="#6B7280"
                                FontWeight="Medium"
                                Text="开药医生" />
                            <TextBox Width="120" CornerRadius="6" Text="{Binding EditDoctor}" />
                        </StackPanel>
                    </StackPanel>
                    
                    <StackPanel Margin="0,8,0,0" Orientation="Horizontal" Spacing="12">
                        <Button
                            Padding="24,8"
                            Background="#10B981"
                            CornerRadius="8"
                            Command="{Binding SaveCommand}"
                            Content="保存"
                            FontWeight="SemiBold"
                            Foreground="White" />
                        <Button
                            Padding="24,8"
                            Background="#9CA3AF"
                            CornerRadius="8"
                            Command="{Binding CancelCommand}"
                            Content="取消"
                            FontWeight="SemiBold"
                            Foreground="White" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
